from setuptools import setup, find_packages

setup(
    name="pylia",
    version="2.0.0",
    description="Python wrapper of LIA",
    author="daigokk",
    packages=find_packages(),
    install_requires=[],
)
